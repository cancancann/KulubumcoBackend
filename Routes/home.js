const express = require("express")
// const getAccessToRoute = require("../middleware/authorization/auth");

const router = express.Router();

// router.get("", getAccessToRoute) // home

module.exports = router;